<form method="post" action="{{ path('product_property_delete', {'id': product_property.id}) }}" onsubmit="return confirm('Вы хотите удалить свойство?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_property.id) }}">
    <button class="btn">Удалить</button>
</form>
