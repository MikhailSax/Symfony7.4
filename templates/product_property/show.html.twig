{% extends '../admin.html.twig' %}

{% block title %}ProductProperty{% endblock %}

{% block body %}
    <h1>ProductProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_property.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ product_property.value }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ product_property.createdAt ? product_property.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ product_property.updatedAt ? product_property.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_property_index') }}">back to list</a>

    <a href="{{ path('product_property_edit', {'id': product_property.id}) }}">edit</a>

    {{ include('product_property/_delete_form.html.twig') }}
{% endblock %}
